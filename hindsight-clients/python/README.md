# Hindsight Python Client

Python client for Hindsight - a semantic memory system with personality-driven thinking.

## Installation

```bash
pip install hindsight-client
```

## Usage

```python
from hindsight_client import HindsightClient

client = HindsightClient(base_url="http://localhost:8080")
```
